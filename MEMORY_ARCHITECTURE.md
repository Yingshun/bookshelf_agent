# 记忆架构

系统采用双重记忆设计：短期记忆负责多轮对话的连贯性，长期记忆负责跨会话的知识留存。

## 短期记忆（当前会话上下文）

由 **LangGraph 的 `messages` 状态 + Gradio `history`** 共同实现。

- Gradio 自动维护当前浏览器会话的对话历史
- 每轮调用时，`agentic_chatbot.py` 中的 `respond()` 将最近 **10 轮**历史转换为 `HumanMessage`/`AIMessage`，连同当前输入一起传入 LangGraph state
- 图内各节点通过 `state["messages"]` 共享完整的多轮上下文
- **生命周期**：仅存在于当前浏览器会话，刷新页面即丢失

## 长期记忆（跨会话持久化）

由 **Mem0 + Qdrant `mem0` collection** 实现。

- **写入**：`generate_answer` 节点每次生成回答后，调用 `mem0.add()` 存入当前轮的问答对。Mem0 内部用 LLM 从对话中提取关键事实和偏好（不是存储原始文本）
- **读取**：`get_user_context` 节点在每轮对话开头，用当前问题调用 `mem0.search()` 做语义检索，将匹配的历史记忆格式化为 `user_context` 注入 system prompt
- **用户隔离**：通过 `mem0_user_id` 区分用户（当前硬编码为 `"user1"`）
- **生命周期**：持久化在 Qdrant 中，跨会话存在

## 两者的协作关系

```
用户提问
  │
  ├─ 短期记忆：最近10轮对话 → 提供即时上下文（"刚才说的那个函数"）
  │
  ├─ 长期记忆：Mem0语义检索 → 补充历史知识（"你之前问过hotplug相关的内容"）
  │
  └─ 两者合并注入 LLM prompt → 生成回答
                                    │
                                    └─ 当前轮问答写入 Mem0 → 沉淀为长期记忆
```

短期记忆解决**连贯性**（多轮对话不断线），长期记忆解决**持久性**（下次来还记得你）。超过 10 轮的早期对话虽然从短期窗口滑出，但其中的关键信息已经被 Mem0 提取存储，仍可通过语义检索召回。
